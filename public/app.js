const header=document.querySelector('.header'),toggle=document.querySelector('.menu-toggle'),nav=document.querySelector('nav');
addEventListener('scroll',()=>header.classList.toggle('scrolled',scrollY>30),{passive:true});toggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',open);toggle.setAttribute('aria-label',open?'Fechar menu':'Abrir menu')});nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));document.querySelector('#year').textContent=new Date().getFullYear();
const pics=[...document.querySelectorAll('.gallery-item img')],labels=[...document.querySelectorAll('.gallery-item span')].map(x=>x.textContent),dialog=document.querySelector('.lightbox'),lightImage=dialog.querySelector('img'),caption=dialog.querySelector('p');let index=0,startX=0;function show(n){index=(n+pics.length)%pics.length;lightImage.src=pics[index].src;lightImage.alt=pics[index].alt;caption.textContent=labels[index]}document.querySelectorAll('.gallery-item').forEach(x=>x.addEventListener('click',()=>{show(+x.dataset.index);dialog.showModal()}));dialog.querySelector('.close').onclick=()=>dialog.close();dialog.querySelector('.previous').onclick=()=>show(index-1);dialog.querySelector('.next').onclick=()=>show(index+1);dialog.addEventListener('click',e=>{if(e.target===dialog)dialog.close()});addEventListener('keydown',e=>{if(!dialog.open)return;if(e.key==='Escape')dialog.close();if(e.key==='ArrowRight')show(index+1);if(e.key==='ArrowLeft')show(index-1)});dialog.addEventListener('touchstart',e=>startX=e.changedTouches[0].screenX,{passive:true});dialog.addEventListener('touchend',e=>{const d=e.changedTouches[0].screenX-startX;if(Math.abs(d)>50)show(index+(d<0?1:-1))},{passive:true});

const heroVideo=document.querySelector('.hero video[data-src]');function startHeroVideo(){if(!heroVideo||matchMedia('(prefers-reduced-motion: reduce)').matches)return;const source=document.createElement('source');source.src=heroVideo.dataset.src;source.type='video/mp4';heroVideo.append(source);heroVideo.load();heroVideo.play().catch(()=>{})}if('requestIdleCallback'in window)requestIdleCallback(startHeroVideo,{timeout:2500});else setTimeout(startHeroVideo,1200);
